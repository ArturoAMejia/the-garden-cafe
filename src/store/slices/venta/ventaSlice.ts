import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const ventaSlice = createSlice({
  name: "venta",
  initialState,
  reducers: {},
});

export const {} = ventaSlice.actions;


